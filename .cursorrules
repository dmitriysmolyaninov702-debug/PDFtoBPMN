# Правила для Cursor AI - BPMN Process Automation Project

## 🎯 Основная цель проекта
Автоматизированная обработка текстов, таблиц и схем для построения BPMN схем бизнес-процессов, совместимых с Camunda Modeler.

---

## 💻 ПРИНЦИПЫ РАЗРАБОТКИ

### Архитектурный подход

**ВСЕГДА перед началом кодирования:**

1. **Провести анализ архитектуры решения:**
   - Определить компоненты и модули системы
   - Выявить зависимости и интерфейсы
   - Оценить сложность и риски
   - Предложить варианты реализации

2. **Согласовать с пользователем:**
   - Представить архитектурное решение
   - Обосновать выбор подхода
   - Получить подтверждение перед кодированием

3. **Только после согласования приступать к реализации**

### Принципы проектирования кода

#### SOLID принципы:
- **S** (Single Responsibility) - Один класс/модуль = одна ответственность
- **O** (Open/Closed) - Открыт для расширения, закрыт для модификации
- **L** (Liskov Substitution) - Подтипы должны заменять базовые типы
- **I** (Interface Segregation) - Много специализированных интерфейсов лучше одного общего
- **D** (Dependency Inversion) - Зависимость от абстракций, а не от конкретных реализаций

#### KISS (Keep It Simple, Stupid):
- Простота предпочтительнее сложности
- Избегать избыточной инженерии
- Выбирать наиболее понятное решение
- Если решение можно упростить - упростить

#### DRY (Don't Repeat Yourself):
- Избегать дублирования кода
- Выносить повторяющуюся логику в функции/модули
- Использовать переиспользуемые компоненты
- Создавать абстракции для общих паттернов

### Последовательность работы над кодом

```
1. 📋 ЗАДАЧА
   └─> Понимание требований

2. 🏗️ АРХИТЕКТУРНЫЙ АНАЛИЗ
   ├─> Проектирование решения
   ├─> Определение компонентов
   ├─> Оценка сложности
   └─> Предложение вариантов

3. 🤝 СОГЛАСОВАНИЕ С ПОЛЬЗОВАТЕЛЕМ
   ├─> Презентация архитектуры
   ├─> Обоснование подхода
   └─> ⚠️ ЖДАТЬ ПОДТВЕРЖДЕНИЯ

4. ⚙️ РЕАЛИЗАЦИЯ (только после согласования!)
   ├─> Применение SOLID/KISS/DRY
   ├─> Написание чистого кода
   └─> Комментирование сложных участков

5. ✅ ВАЛИДАЦИЯ
   ├─> Проверка работоспособности
   ├─> Соответствие принципам
   └─> Документирование
```

### Формат представления архитектуры

```markdown
🏗️ АРХИТЕКТУРНЫЙ АНАЛИЗ

## Задача
[Описание задачи своими словами]

## Предлагаемая архитектура

### Компоненты:
1. [Компонент 1]
   - Ответственность: [...]
   - Интерфейс: [...]
   
2. [Компонент 2]
   - Ответственность: [...]
   - Интерфейс: [...]

### Схема взаимодействия:
[Текстовая или ASCII схема]

### Применение принципов:
- SOLID: [как применяется]
- KISS: [как соблюдается простота]
- DRY: [как избегается дублирование]

### Альтернативные варианты:
1. [Вариант 1] - [плюсы/минусы]
2. [Вариант 2] - [плюсы/минусы]

### Рекомендация:
[Обоснованный выбор подхода]

## Требуется ваше согласие для начала реализации
```

---

## 🌟 МЕТОДОЛОГИЯ ЭКСПЕРТНОГО ПРОЦЕССНОГО МОДЕЛИРОВАНИЯ

### Уровень 0: Экспертиза
**ВСЕГДА работать на уровне МИРОВОГО ЭКСПЕРТА по процессному моделированию:**
- Применять лучшие практики BPMN 2.0 и нотации моделирования
- Использовать методологии BPM (Business Process Management)
- Следовать стандартам OMG (Object Management Group)
- Учитывать принципы Six Sigma, Lean, ITIL при анализе процессов
- Применять паттерны workflow и best practices индустрии

---

## 🔄 ПОСЛЕДОВАТЕЛЬНОСТЬ РАБОТЫ НАД ПРОЦЕССОМ

### Этап 1: АНАЛИЗ И СТРУКТУРИРОВАНИЕ 📊

#### 1.1 Анализ исходных документов
При получении документа выполнить:

1. **Идентификация типа процесса:**
   - Операционный (повторяющийся, стандартный)
   - Управленческий (принятие решений, планирование)
   - Поддерживающий (обеспечение основной деятельности)

2. **Определение границ процесса:**
   - Триггер (событие запуска)
   - Входы (данные, документы, условия)
   - Выходы (результаты, артефакты)
   - Окончание (критерии завершения)

3. **Выявление ключевых разделов:**
   - По фазам жизненного цикла
   - По функциональным областям
   - По временным периодам
   - По степени детализации

4. **Оценка объема и сложности:**
   - Количество участников (ролей)
   - Количество шагов/действий
   - Количество точек принятия решений
   - Уровень интеграции с другими процессами

#### 1.2 Предложение структуры работы

**ОБЯЗАТЕЛЬНО предложить пользователю структуру в формате:**

```
📋 ПРЕДЛАГАЕМАЯ СТРУКТУРА РАБОТЫ

Процесс: [Название процесса]
Тип: [Операционный/Управленческий/Поддерживающий]

Предлагаемое разделение на части:

ЧАСТЬ 1: [Название раздела]
- Описание: [краткое описание фазы]
- Примерное количество элементов: [число]
- Ключевые роли: [перечень]
- Оценка сложности: [низкая/средняя/высокая]

ЧАСТЬ 2: [Название раздела]
...

РЕКОМЕНДАЦИЯ ПО РАБОТЕ:
- Начать с: [рекомендация]
- Детализация: [уровень детализации]
- Ожидаемое время: [оценка]

Согласны с такой структурой? Нужны корректировки?
```

**ПРИНЦИПЫ структурирования:**
- ❗ ИЗБЕГАТЬ ПЕРЕГРУЗКИ КОНТЕКСТА - разбивать на логические части по 5-10 элементов
- Каждая часть должна иметь логическую завершенность
- Минимизировать связи между частями для независимой работы
- Учитывать естественные boundaries процесса

---

### Этап 2: МАТРИЦА ОТВЕТСТВЕННОСТИ RACI 👥

#### 2.1 Построение матрицы RACI

**ОБЯЗАТЕЛЬНО создать отдельный файл:**
- **Формат**: `[Название_процесса]_RACI.md`
- **Пример**: `Жизненный_цикл_договора_RACI.md`

**Для КАЖДОГО раздела создать матрицу RACI:**

| Activity / Задача | Роль 1 | Роль 2 | Роль 3 | Роль 4 |
|-------------------|--------|--------|--------|--------|
| Задача 1 | R | A | C | I |
| Задача 2 | A | R | I | C |

**Обозначения:**
- **R** (Responsible) - Исполнитель, выполняет работу
- **A** (Accountable) - Ответственный, принимает решения, отвечает за результат
- **C** (Consulted) - Консультант, предоставляет экспертизу
- **I** (Informed) - Информируемый, получает информацию о результатах

#### 2.2 Определение ролевой модели

**ОБЯЗАТЕЛЬНО согласовать с пользователем:**

```
🎭 РОЛЕВАЯ МОДЕЛЬ ПРОЦЕССА

Выявленные роли:
1. [Роль 1] - [Описание функций]
2. [Роль 2] - [Описание функций]
3. [Роль 3] - [Описание функций]

Предлагаемые должности:
- [Роль 1] → [Должность] (например: "Руководитель ПФМ")
- [Роль 2] → [Должность] (например: "Ответственный сотрудник")

СТРУКТУРА POOL И LANES:

POOL: [Название организации/процесса]
├── LANE 1: [Название роли/должности]
│   └── [Краткое описание ответственности]
├── LANE 2: [Название роли/должности]
│   └── [Краткое описание ответственности]
└── LANE 3: [Название роли/должности]
    └── [Краткое описание ответственности]

Согласны с такой структурой ролей и lanes? Требуются изменения?
```

**ПРАВИЛА определения lanes:**
- Каждая lane = одна роль или подразделение
- Lane должна содержать только те задачи, за которые роль имеет R или A
- Избегать слишком дробного разделения (оптимально 3-8 lanes)
- Учитывать организационную структуру компании

---

### Этап 3: ТЕКСТОВЫЙ ПАЙПЛАЙН ПРОЦЕССА 📝

**ОБЯЗАТЕЛЬНО создать отдельный файл:**
- **Формат**: `[Название_процесса]_Pipeline.md`
- **Пример**: `Жизненный_цикл_договора_Pipeline.md`

#### 3.1 Структура описания каждого Activity

**Для КАЖДОГО элемента процесса подготовить:**

```
═══════════════════════════════════════════════════════════
ACTIVITY: [Номер] - [Название задачи]
═══════════════════════════════════════════════════════════

1️⃣ НАЗВАНИЕ И ТИП ВЫПОЛНЕНИЯ:
   Название: [Полное название activity]
   ID: [Task_ID]
   Тип элемента: [Task / User Task / Service Task / Manual Task]
   
   Способ выполнения:
   🤖 [АВТОМАТИЗИРОВАН] - выполняется системой автоматически
      ИЛИ
   👤 [РУЧНАЯ ОПЕРАЦИЯ] - требует участия человека
      ИЛИ
   🔄 [ПОЛУАВТОМАТИЗИРОВАН] - система + человек
   
   Степень автоматизации: [0-100%]
   Описание: [краткое описание что делается]

2️⃣ ОТВЕТСТВЕННЫЙ:
   RACI роль: [R - Исполнитель / A - Ответственный]
   Должность: [Название должности]
   Подразделение: [Название подразделения]
   Lane: [Название lane в BPMN]
   
   Дополнительные участники:
   - [C] Консультанты: [перечень]
   - [I] Информируемые: [перечень]

3️⃣ ВХОДЫ И ВЫХОДЫ:
   Входящие данные:
   - [Название документа/данных] (формат: [тип], источник: [откуда])
   - [Название данных] (формат: [тип], источник: [откуда])
   
   Исходящие данные:
   - [Название результата] (формат: [тип], получатель: [кому])
   - [Название артефакта] (формат: [тип], получатель: [кому])
   
   Способы передачи данных:
   - Между задачами: [ИС, Email, Физически, API, Файлообменник]
   - Интеграции: [Название ИС → Название ИС, протокол]
   - Документооборот: [Бумажный / Электронный / Гибридный]

4️⃣ ВРЕМЕННЫЕ ХАРАКТЕРИСТИКИ:
   Плановое время исполнения: [X рабочих дней / часов]
   Минимальное время: [X]
   Максимальное время: [X]
   Критический срок: [X] (при превышении - эскалация)
   
   Зависимости:
   - Предшествующие задачи: [список]
   - Последующие задачи: [список]
   - Ожидание внешних событий: [если есть]

5️⃣ МЕТРИКИ И KPI:
   Метрики исполнения:
   ✓ Соблюдение срока: [Да/Нет, % вовремя]
   ✓ Качество результата: [критерии оценки]
   ✓ Количество итераций: [среднее число доработок]
   ✓ Загрузка исполнителя: [FTE, %]
   
   KPI процесса (если применимо):
   - [Название KPI]: целевое значение [X], формула расчета
   - [Название KPI]: целевое значение [X], формула расчета
   
   Триггеры эскалации:
   - [Условие] → [Действие]
   - [Условие] → [Действие]

6️⃣ УСЛОВИЯ И ПРАВИЛА:
   Бизнес-правила:
   - [Правило 1]: [описание]
   - [Правило 2]: [описание]
   
   Условия выполнения:
   - Обязательные: [что должно быть выполнено перед стартом]
   - Желательные: [что улучшит выполнение]
   
   Критерии качества:
   - [Критерий]: [как проверяется]

7️⃣ РИСКИ И ИСКЛЮЧЕНИЯ:
   Типичные проблемы:
   - [Проблема] → [Способ решения]
   
   Исключительные ситуации:
   - [Ситуация] → [Path обработки]

═══════════════════════════════════════════════════════════
```

#### 3.2 Пайплайн процесса целиком

**После описания всех activities создать сводный пайплайн:**

```
🔄 ПОЛНЫЙ ПАЙПЛАЙН ПРОЦЕССА: [Название]

START → [Activity 1] → [Gateway?] → [Activity 2] → ... → END
         ↓ (5 дней)    ↓ (решение)  ↓ (3 дня)
       [Роль 1]        [Роль 2]      [Роль 3]
       [Ручная]        [Авто]        [Ручная]

ПОСЛЕДОВАТЕЛЬНОСТЬ:
1. [Activity 1] (Роль, Время, Тип) → передача [данные] через [способ]
2. [Activity 2] (Роль, Время, Тип) → передача [данные] через [способ]
...

ОБЩИЕ МЕТРИКИ ПРОЦЕССА:
- Lead Time (общее время): [X дней]
- Process Time (активное время): [Y дней]
- Эффективность (Process Time / Lead Time): [Z%]
- Стоимость процесса: [сумма]
- Качество (First Pass Yield): [%]
```

---

### Этап 4: СОЗДАНИЕ BPMN И ДОКУМЕНТАЦИИ 🎨

**ОБЯЗАТЕЛЬНО создать ДВА связанных файла:**

1. **BPMN XML файл:**
   - **Формат**: `[Название_процесса].bpmn`
   - **Пример**: `Жизненный_цикл_договора.bpmn`
   - **Содержит**: Структуру процесса в формате BPMN 2.0 для Camunda Modeler

2. **MD документация:**
   - **Формат**: `[Название_процесса].md` (ТО ЖЕ ИМЯ, что и BPMN!)
   - **Пример**: `Жизненный_цикл_договора.md`
   - **Содержит**: Таблицы с детальным описанием каждого элемента процесса

**ПРАВИЛА СВЯЗИ:**
- Имена файлов ДОЛЖНЫ совпадать (кроме расширения)
- Названия элементов в MD ДОЛЖНЫ точно соответствовать атрибуту `name` в BPMN
- Каждому элементу в BPMN соответствует строка в таблице MD

**Последовательность работы:**
1. Генерация BPMN XML файла (по техническим правилам ниже)
2. Создание MD документации с таблицами
3. Валидация в Camunda Modeler
4. Проверка соответствия имен элементов

---

## 📋 Общие правила

### Язык коммуникации
- Всегда отвечать на русском языке
- Использовать профессиональную терминологию в области BPM
- Пояснять технические термины при первом упоминании

### Стиль работы
- **Архитектурный подход**: Всегда начинать с анализа и согласования решения
- **SOLID/KISS/DRY**: Применять принципы качественной разработки
- Действовать проактивно ТОЛЬКО после согласования архитектуры
- Создавать резервные копии в папке `archive/` перед значительными изменениями
- Документировать все изменения и решения
- **Вести Changelog**: Фиксировать все значимые изменения в `Changelog.md`
- **❌ НЕ делать git коммиты**: Пользователь делает коммиты самостоятельно

### ❗ ВАЖНО: Работа с документацией

**У нас развитая архитектура документации:**
- `README.md` - главная точка входа, навигация
- `Changelog.md` - история изменений
- `docs/Architecture.md` - техническая архитектура
- `docs/DeepSeek_OCR_Setup.md` - инструкции по установке
- `.cursorrules` - методология работы

**ПРАВИЛА:**
1. **❌ ЗАПРЕЩЕНО создавать MD файлы без ЯВНОЙ команды пользователя**
   - Отчеты, статусы, summary, guides, recommendations, findings - НЕТ!
   - ВСЕ выводы, анализы, результаты → **В ЧАТ**
   - MD файлы ТОЛЬКО когда пользователь ЯВНО скажет "создай MD файл" или "задокументируй в файле"
   - Важная информация → `Changelog.md` (по окончании работы)

2. **Архитектуру документации менять ТОЛЬКО:**
   - При обнаружении пробела в документации
   - С явного разрешения пользователя
   - После согласования структуры

3. **Исключения (часть методологии работы с процессами):**
   - `[Процесс]_RACI.md` - матрица ответственности
   - `[Процесс]_Pipeline.md` - текстовый пайплайн
   - `[Процесс].bpmn` - BPMN диаграмма
   - `[Процесс].md` - документация процесса

**ФОРМАТ ВЫВОДА:**
- По умолчанию: весь анализ, выводы, рекомендации → в чат
- MD файлы: только по явной команде пользователя
- Changelog: обновлять в конце работы над задачей

### Последовательность работы с новыми задачами
1. 📋 Анализ требований
2. 🏗️ Проектирование архитектуры решения
3. 🤝 **ОБЯЗАТЕЛЬНОЕ согласование с пользователем**
4. ⚙️ Реализация (только после подтверждения)
5. ✅ Валидация и документирование
6. 📝 Обновление Changelog

### Ведение Changelog

**ОБЯЗАТЕЛЬНО обновлять файл `Changelog.md` при каждом значимом изменении проекта.**

#### Формат записи в Changelog.md:

```markdown
## [DD-MM-YYYY] - Краткое описание изменений

### Добавлено
- Описание новых функций
- Новые возможности

### Изменено
- Описание модификаций существующего функционала
- Улучшения и оптимизации

### Исправлено
- Описание исправленных ошибок
- Решенные проблемы
```

#### Правила ведения Changelog:

1. **Дата в формате**: `DD-MM-YYYY` (например: 28-10-2025)
2. **Краткое описание**: Суть изменений одной фразой
3. **Категории изменений**:
   - **Добавлено** - новые функции, компоненты, файлы
   - **Изменено** - модификации существующего кода, обновления
   - **Исправлено** - исправление ошибок, багфиксы
4. **Порядок записей**: От новых к старым (последние изменения наверху)
5. **Детальность**: Достаточно для понимания сути изменения

#### Примеры записей:

```markdown
## [28-10-2025] - Добавлены принципы SOLID/KISS/DRY

### Добавлено
- Раздел "Принципы разработки" в .cursorrules
- Архитектурный подход с обязательным согласованием
- Формат представления архитектурного анализа
- Чеклист этапа 0 для архитектурного анализа

### Изменено
- Обновлен README.md с описанием принципов разработки
- Расширен раздел "Общие правила" в .cursorrules

## [28-10-2025] - Создана система связанных имен файлов

### Добавлено
- Правила именования 4 файлов для каждого процесса
- Структура папок с примерами для Changelog
- Раздел "Связь файлов" в чеклисте

### Изменено
- Обновлена структура проекта в README.md
- Добавлены визуальные схемы связей файлов
```

---

## 📊 ПОДДЕРЖИВАЕМЫЕ ЭЛЕМЕНТЫ BPMN 2.0

**Источники:**
- OMG BPMN 2.0.2 Specification  
- КД-СТ-161-01 "Требования к моделированию в Business Studio"  
- **Полный справочник:** `docs/BPMN_Elements_Reference.md`

### Принципы использования элементов BPMN

1. **Соответствие стандарту:** Все элементы должны соответствовать OMG BPMN 2.0
2. **Совместимость:** Схемы должны открываться в Camunda Modeler без ошибок
3. **Документирование:** При использовании расширенных элементов ОБЯЗАТЕЛЬНО указывать в отчете
4. **Приоритет:** Использовать наиболее простые и понятные элементы (KISS принцип)

---

### 1. ACTIVITIES (Активности)

#### Tasks (Задачи)

**Основные (используются часто):**
- `<bpmn:task>` - **Abstract Task** - Абстрактная задача
- `<bpmn:userTask>` - **User Task** - Задача пользователя (человек + UI)
- `<bpmn:manualTask>` - **Manual Task** - Ручное выполнение (без IT-систем)
- `<bpmn:serviceTask>` - **Service Task** - Сервисная задача (автоматизация)

**Дополнительные (по необходимости):**
- `<bpmn:scriptTask>` - **Script Task** - Выполнение скрипта
- `<bpmn:businessRuleTask>` - **Business Rule Task** - Выполнение бизнес-правил
- `<bpmn:sendTask>` - **Send Task** - Отправка сообщения
- `<bpmn:receiveTask>` - **Receive Task** - Получение сообщения

**Именование задач:** Глагол + объект  
**Примеры:** "Подготовить документ", "Согласовать условия"

#### Subprocesses (Подпроцессы)

- `<bpmn:subProcess>` - **Subprocess** - Обычный подпроцесс
  - `collapsed="true"` - свернутый
  - `collapsed="false"` - развернутый
- `<bpmn:subProcess triggeredByEvent="true">` - **Event Subprocess** - Событийный
- `<bpmn:transaction>` - **Transaction** - Транзакция (атомарность)
- `<bpmn:adHocSubProcess>` - **Ad-Hoc** - Гибкий порядок задач
- `<bpmn:callActivity>` - **Call Activity** - Ссылка на типовой процесс

#### Loops (Циклы)

- `<bpmn:standardLoopCharacteristics>` - **Standard Loop** - Цикл с условием
- `<bpmn:multiInstanceLoopCharacteristics isSequential="false">` - **Parallel Multi-Instance**
- `<bpmn:multiInstanceLoopCharacteristics isSequential="true">` - **Sequential Multi-Instance**

---

### 2. EVENTS (События)

#### Start Events (Стартовые события)

**Основные:**
- `<bpmn:startEvent>` - **None Start** - Обычное стартовое
- `<bpmn:startEvent><bpmn:messageEventDefinition />` - **Message Start** - По сообщению
- `<bpmn:startEvent><bpmn:timerEventDefinition />` - **Timer Start** - По времени

**Дополнительные:**
- `<bpmn:startEvent><bpmn:conditionalEventDefinition />` - **Conditional Start** - По условию
- `<bpmn:startEvent><bpmn:signalEventDefinition />` - **Signal Start** - По сигналу
- `<bpmn:startEvent parallelMultiple="false"><bpmn:multipleEventDefinition />` - **Multiple** - ЛЮБОЕ событие
- `<bpmn:startEvent parallelMultiple="true"><bpmn:multipleEventDefinition />` - **Parallel Multiple** - ВСЕ события

**Именование событий:** Объект + краткая форма причастия  
**Примеры:** "Заявка получена", "Договор подписан", "Срок истек"

#### Intermediate Events (Промежуточные события)

**Catch Events (Обработчики - ожидание):**
- `<bpmn:intermediateCatchEvent><bpmn:messageEventDefinition />` - Ожидание сообщения
- `<bpmn:intermediateCatchEvent><bpmn:timerEventDefinition />` - Ожидание времени
- `<bpmn:intermediateCatchEvent><bpmn:conditionalEventDefinition />` - Ожидание условия
- `<bpmn:intermediateCatchEvent><bpmn:signalEventDefinition />` - Ожидание сигнала

**Throw Events (Инициаторы - генерация):**
- `<bpmn:intermediateThrowEvent><bpmn:messageEventDefinition />` - Отправка сообщения
- `<bpmn:intermediateThrowEvent><bpmn:signalEventDefinition />` - Отправка сигнала
- `<bpmn:intermediateThrowEvent><bpmn:escalationEventDefinition />` - Эскалация
- `<bpmn:intermediateThrowEvent><bpmn:compensateEventDefinition />` - Компенсация

#### End Events (Конечные события)

**Основные:**
- `<bpmn:endEvent>` - **None End** - Обычное завершение
- `<bpmn:endEvent><bpmn:messageEventDefinition />` - **Message End** - Завершение с сообщением

**Дополнительные:**
- `<bpmn:endEvent><bpmn:errorEventDefinition />` - **Error End** - Завершение с ошибкой
- `<bpmn:endEvent><bpmn:escalationEventDefinition />` - **Escalation End** - Эскалация
- `<bpmn:endEvent><bpmn:cancelEventDefinition />` - **Cancel End** - Отмена (только в Transaction)
- `<bpmn:endEvent><bpmn:compensateEventDefinition />` - **Compensation End** - Компенсация
- `<bpmn:endEvent><bpmn:signalEventDefinition />` - **Signal End** - Сигнал
- `<bpmn:endEvent><bpmn:terminateEventDefinition />` - **Terminate End** - Принудительное завершение всего

**Именование:** Результат  
**Примеры:** "Договор заключен", "Процесс завершен", "Заявка отклонена"

---

### 3. GATEWAYS (Шлюзы)

**Основные (используются часто):**
- `<bpmn:exclusiveGateway>` - **Exclusive Gateway (XOR)** - Исключающее ИЛИ
  - Ветвление: ТОЛЬКО одна ветвь (первая выполнившаяся)
  - Слияние: ожидает ЛЮБОЙ входящий поток
  - **Именование (ветвление):** Вопрос с условием
  - **Примеры:** "Бюджет согласован?", "Сумма > 1 млн?"

- `<bpmn:parallelGateway>` - **Parallel Gateway (AND)** - Параллельный
  - Ветвление: ВСЕ ветви одновременно
  - Слияние: ожидает ВСЕ входящие потоки
  - **Не подписывается**

**Дополнительные:**
- `<bpmn:inclusiveGateway>` - **Inclusive Gateway (OR)** - Неэксклюзивный
  - Ветвление: все ветви с выполненными условиями
  - Слияние: ожидает все активированные ветви
  - **Именование:** Можно для ветвления, слияние не подписывается

- `<bpmn:eventBasedGateway>` - **Event-Based Gateway** - На основе событий
  - Выбор ветви по первому произошедшему событию
  - После шлюза - промежуточные события
  - **Не подписывается**

- `<bpmn:complexGateway>` - **Complex Gateway** - Сложный (редко)

**Примечание:** Шлюзы слияния НЕ подписываются!

---

### 4. CONNECTING OBJECTS (Связующие объекты)

- `<bpmn:sequenceFlow>` - **Sequence Flow** - Последовательный поток
  - Связывает элементы ВНУТРИ одного процесса
  - Атрибуты: `sourceRef`, `targetRef`, `name` (условие)

- `<bpmn:messageFlow>` - **Message Flow** - Поток сообщений
  - Связывает элементы МЕЖДУ разными Pool
  - НЕ может связывать элементы внутри одного Pool

- `<bpmn:association>` - **Association** - Ассоциация
  - Связь артефактов с элементами процесса
  - Типы: направленная (→), ненаправленная (-)

---

### 5. SWIMLANES (Дорожки)

#### Pool (Пул)
- `<bpmn:participant processRef="Process_ID">` - **Pool**
  - Представляет организацию/участника процесса
  - Типы: развернутый (с диаграммой), свернутый ("black box")
  - **Примеры:** "ПАО «ЮТэйр»", "Контрагент", "Клиент"

#### Lane (Дорожка)
- `<bpmn:lane>` - **Lane**
  - Разделение Pool по ролям/подразделениям
  - Оптимально: 1-5 lanes, максимум: 7
  - **Примеры:** "Руководитель отдела", "Специалист", "Бухгалтерия"

---

### 6. ARTIFACTS (Артефакты)

- `<bpmn:dataObject>` - **Data Object** - Объект данных (документ, информация)
- `<bpmn:dataStoreReference>` - **Data Store** - Хранилище данных (БД, CRM)
- `<bpmn:textAnnotation>` - **Text Annotation** - Текстовая аннотация (комментарий)
- `<bpmn:group>` - **Group** - Группа (визуальное объединение)

---

### 📋 Рекомендации по оформлению (КД-СТ-161-01)

**Формат диаграммы:**
- Ориентация: Альбомная
- Размер: А4
- Направление: Слева направо (временная последовательность)

**Количество элементов:**
- Исполнители (lanes): 1-5 (максимум 7)
- Задачи (tasks): 3-15 (максимум 20)

**Правила размещения:**
- Задачи располагаются в соответствующих дорожках исполнителей
- Элементы идут слева направо по временной последовательности
- Избегать пересечений потоков (sequence flows)

---

### ⚠️ ВАЖНО: Документирование новых элементов

При использовании элементов, не описанных в основном README.md:

1. **В отчете ОБЯЗАТЕЛЬНО указать:**
   - Название элемента BPMN
   - XML тег
   - Зачем используется
   - Ссылка на `docs/BPMN_Elements_Reference.md`

2. **В MD документации процесса:**
   - Создать отдельную таблицу для новых типов элементов
   - Подробно описать семантику и назначение
   - Примеры использования

3. **В Changelog:**
   - Зафиксировать использование расширенных элементов
   - Обосновать необходимость

**Пример отчета:**
```markdown
⚠️ ИСПОЛЬЗОВАНЫ РАСШИРЕННЫЕ ЭЛЕМЕНТЫ BPMN:

1. **Event-Based Gateway** (`<bpmn:eventBasedGateway>`)
   - Причина: Необходимо ожидать одно из двух событий (ответ клиента ИЛИ истечение срока)
   - Местоположение: После задачи "Отправить запрос клиенту"
   - Справочник: docs/BPMN_Elements_Reference.md, раздел 3.4

2. **Timer Intermediate Catch Event** (`<bpmn:intermediateCatchEvent><bpmn:timerEventDefinition />`)
   - Причина: Ожидание 5 рабочих дней перед эскалацией
   - Местоположение: В ветви ожидания ответа
   - Справочник: docs/BPMN_Elements_Reference.md, раздел 2.2.1.2
```

---

### 📈 Приоритет использования элементов

**Часто (80% случаев):**
- User Task, Service Task, Manual Task
- Exclusive Gateway, Parallel Gateway
- None/Message Start/End Events
- Sequence Flow, Message Flow
- Pool, Lane

**Средне (15% случаев):**
- Subprocess, Call Activity
- Timer Events, Conditional Events
- Inclusive Gateway, Event-Based Gateway
- Data Objects, Text Annotations

**Редко (5% случаев):**
- Transaction, Ad-Hoc Subprocess
- Signal/Escalation Events
- Complex Gateway
- Script/Business Rule Tasks

---

## 🔧 Работа с BPMN XML файлами

### КРИТИЧЕСКИ ВАЖНО: Структура BPMN XML

#### 1. Обязательные элементы для ВСЕХ объектов процесса

```xml
<!-- Для каждого элемента (Event, Task, Gateway) ОБЯЗАТЕЛЬНО создавать: -->

<!-- 1. Определение элемента в процессе -->
<bpmn:task id="Task_Example" name="Название задачи">
  <bpmn:incoming>Flow_1</bpmn:incoming>
  <bpmn:outgoing>Flow_2</bpmn:outgoing>
</bpmn:task>

<!-- 2. Визуальное представление (BPMNShape) -->
<bpmndi:BPMNShape id="Task_Example_di" bpmnElement="Task_Example">
  <dc:Bounds x="100" y="100" width="100" height="80" />
</bpmndi:BPMNShape>

<!-- 3. Для потоков (Sequence Flow) - визуальные связи (BPMNEdge) -->
<bpmndi:BPMNEdge id="Flow_1_di" bpmnElement="Flow_1">
  <di:waypoint x="50" y="140" />
  <di:waypoint x="100" y="140" />
</bpmndi:BPMNEdge>
```

**БЕЗ bpmndi:BPMNShape и bpmndi:BPMNEdge элементы НЕ БУДУТ ВИДНЫ в Camunda Modeler!**

#### 2. Координаты и размеры

**Стандартные размеры элементов:**
- **Task (Задача)**: width="100" height="80"
- **Gateway (Шлюз)**: width="50" height="50"
- **Start Event**: width="36" height="36"
- **End Event**: width="36" height="36"
- **Pool**: width="3600" height="1200" (или больше, в зависимости от содержимого)
- **Lane**: width="3570" height="200" (ширина = ширина Pool - 30)

**Расположение:**
- Горизонтальное расстояние между элементами: 160-200px
- Вертикальное расстояние между дорожками: 200px
- Начальное смещение в Pool: x="190" (оставить место для названия lane)

#### 3. Правильная структура XML

```xml
<?xml version="1.0" encoding="UTF-8"?>
<bpmn:definitions xmlns:bpmn="http://www.omg.org/spec/BPMN/20100524/MODEL" 
                  xmlns:bpmndi="http://www.omg.org/spec/BPMN/20100524/DI" 
                  xmlns:dc="http://www.omg.org/spec/DD/20100524/DC" 
                  xmlns:di="http://www.omg.org/spec/DD/20100524/DI" 
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
                  id="Definitions_1" 
                  targetNamespace="http://bpmn.io/schema/bpmn">
  
  <bpmn:collaboration id="Collaboration_1">
    <bpmn:participant id="Participant_1" name="Название процесса" processRef="Process_1" />
  </bpmn:collaboration>
  
  <bpmn:process id="Process_1" name="Процесс" isExecutable="true">
    <bpmn:laneSet id="LaneSet_1">
      <bpmn:lane id="Lane_1" name="Дорожка 1">
        <bpmn:flowNodeRef>Task_1</bpmn:flowNodeRef>
      </bpmn:lane>
    </bpmn:laneSet>
    
    <!-- Определения элементов процесса -->
    <bpmn:startEvent id="StartEvent_1" name="Старт">
      <bpmn:outgoing>Flow_1</bpmn:outgoing>
    </bpmn:startEvent>
    
    <!-- Sequence Flows -->
    <bpmn:sequenceFlow id="Flow_1" sourceRef="StartEvent_1" targetRef="Task_1" />
  </bpmn:process>
  
  <!-- ОБЯЗАТЕЛЬНАЯ СЕКЦИЯ ВИЗУАЛИЗАЦИИ -->
  <bpmndi:BPMNDiagram id="BPMNDiagram_1">
    <bpmndi:BPMNPlane id="BPMNPlane_1" bpmnElement="Collaboration_1">
      
      <!-- Shape для Participant -->
      <bpmndi:BPMNShape id="Participant_1_di" bpmnElement="Participant_1" isHorizontal="true">
        <dc:Bounds x="160" y="80" width="3600" height="1200" />
      </bpmndi:BPMNShape>
      
      <!-- Shapes для Lanes -->
      <bpmndi:BPMNShape id="Lane_1_di" bpmnElement="Lane_1" isHorizontal="true">
        <dc:Bounds x="190" y="80" width="3570" height="200" />
      </bpmndi:BPMNShape>
      
      <!-- Shapes для элементов -->
      <bpmndi:BPMNShape id="StartEvent_1_di" bpmnElement="StartEvent_1">
        <dc:Bounds x="252" y="152" width="36" height="36" />
        <bpmndi:BPMNLabel>
          <dc:Bounds x="258" y="195" width="24" height="14" />
        </bpmndi:BPMNLabel>
      </bpmndi:BPMNShape>
      
      <!-- Edges для потоков -->
      <bpmndi:BPMNEdge id="Flow_1_di" bpmnElement="Flow_1">
        <di:waypoint x="288" y="170" />
        <di:waypoint x="340" y="170" />
      </bpmndi:BPMNEdge>
      
    </bpmndi:BPMNPlane>
  </bpmndi:BPMNDiagram>
</bpmn:definitions>
```

### ЧАСТЫЕ ОШИБКИ И КАК ИХ ИЗБЕГАТЬ

#### ❌ ОШИБКА 1: Дублирование закрывающих тегов
```xml
<!-- НЕПРАВИЛЬНО -->
</bpmn:definitions>
</bpmn:definitions>  <!-- ДУБЛИКАТ! -->

<!-- ПРАВИЛЬНО -->
</bpmn:definitions>  <!-- Только один закрывающий тег -->
```

#### ❌ ОШИБКА 2: Отсутствие визуальных элементов
```xml
<!-- НЕПРАВИЛЬНО - элемент не будет виден -->
<bpmn:task id="Task_1" name="Задача">
  <bpmn:incoming>Flow_1</bpmn:incoming>
  <bpmn:outgoing>Flow_2</bpmn:outgoing>
</bpmn:task>
<!-- Нет соответствующего BPMNShape! -->

<!-- ПРАВИЛЬНО -->
<bpmn:task id="Task_1" name="Задача">
  <bpmn:incoming>Flow_1</bpmn:incoming>
  <bpmn:outgoing>Flow_2</bpmn:outgoing>
</bpmn:task>

<!-- В секции bpmndi:BPMNDiagram: -->
<bpmndi:BPMNShape id="Task_1_di" bpmnElement="Task_1">
  <dc:Bounds x="300" y="130" width="100" height="80" />
</bpmndi:BPMNShape>
```

#### ❌ ОШИБКА 3: Неправильные ссылки
```xml
<!-- НЕПРАВИЛЬНО -->
<bpmn:lane id="Lane_1">
  <bpmn:flowNodeRef>Task_Wrong</bpmn:flowNodeRef>  <!-- Такого элемента нет! -->
</bpmn:lane>

<!-- ПРАВИЛЬНО -->
<bpmn:task id="Task_1" name="Задача" />

<bpmn:lane id="Lane_1">
  <bpmn:flowNodeRef>Task_1</bpmn:flowNodeRef>  <!-- Соответствует id элемента -->
</bpmn:lane>
```

#### ❌ ОШИБКА 4: Использование документации в BPMN
```xml
<!-- НЕ РЕКОМЕНДУЕТСЯ - раздувает XML -->
<bpmn:task id="Task_1" name="Задача">
  <bpmn:documentation>Очень длинное описание задачи...</bpmn:documentation>
  <bpmn:incoming>Flow_1</bpmn:incoming>
</bpmn:task>

<!-- ПРАВИЛЬНО - выносить в отдельный MD файл -->
<!-- В BPMN только структура процесса -->
<bpmn:task id="Task_1" name="Задача">
  <bpmn:incoming>Flow_1</bpmn:incoming>
</bpmn:task>

<!-- В файле process_name.md создать таблицу: -->
| Название элемента | Описание |
|-------------------|----------|
| Задача            | Очень длинное описание задачи... |
```

## 📄 Работа с документацией процессов

### Структура MD файлов

```markdown
# Название процесса

## Таблица 1. События процесса

| Название элемента | Тип | Описание и условия выполнения |
|-------------------|-----|-------------------------------|
| Старт процесса | Start Event | Описание... |

## Таблица 2. Шлюзы принятия решений

| Название элемента | Тип | Описание и условия выполнения |
|-------------------|-----|-------------------------------|
| Проверка условия | Exclusive Gateway | УСЛОВИЕ: если... то... |

## Таблица 3. Задачи в дорожке "Роль"

| Название элемента | Ответственный | Срок | Описание |
|-------------------|---------------|------|----------|
| Выполнить действие | Исполнитель | 5 дней | Детали... |
```

### Правила для таблиц

1. **Названия элементов** в таблице ДОЛЖНЫ точно соответствовать атрибуту `name` в BPMN XML
2. **Группировка** по типам элементов и дорожкам (lanes)
3. **Включать**: срок выполнения, ответственного, ссылки на пункты регламента
4. **Форматирование**: использовать жирный текст для ключевых слов (УСЛОВИЕ, СРОК, ОТВЕТСТВЕННОСТЬ)

## 📥 Обработка входных документов

### PDF файлы
- Проверять необходимость OCR (если текст не извлекается)
- Сохранять структуру документа (заголовки, списки, таблицы)
- Извлекать номера пунктов и ссылки

### DOCX файлы
- Парсить структуру: заголовки, абзацы, таблицы, списки
- Определять иерархию разделов
- Извлекать форматирование (жирный текст = ключевые понятия)

### XLSX файлы
- Анализировать связи между ячейками
- Определять последовательность действий из строк/столбцов
- Извлекать условия из формул

### TXT файлы
- Определять структуру по отступам и нумерации
- Искать ключевые слова процессов (если, то, далее, затем)

## 🎨 Генерация BPMN из текста

### Определение элементов процесса

**Ключевые слова для Task:**
- "выполнить", "подготовить", "создать", "проверить", "согласовать"
- "оформить", "направить", "подписать", "разместить"

**Ключевые слова для Gateway:**
- "если", "в случае", "при условии", "проверка"
- "да/нет", "соответствует/не соответствует"

**Ключевые слова для Start Event:**
- "начало", "инициирование", "запуск", "старт процесса"

**Ключевые слова для End Event:**
- "завершение", "конец", "процесс завершен", "финал"

### Организация в Lanes

**Определение ролей/подразделений:**
- Искать упоминания должностей: "руководитель", "специалист", "юрист"
- Искать названия подразделений: "отдел", "управление", "служба"
- Группировать действия по ответственным

## 🔍 Валидация BPMN

### Обязательные проверки перед сохранением

1. ✅ Каждый элемент процесса имеет соответствующий BPMNShape
2. ✅ Каждый Sequence Flow имеет соответствующий BPMNEdge
3. ✅ Все flowNodeRef в lanes указывают на существующие элементы
4. ✅ sourceRef и targetRef в sequenceFlow указывают на существующие элементы
5. ✅ Нет дублирующих закрывающих тегов
6. ✅ Все namespace объявлены в корневом элементе
7. ✅ Координаты элементов не перекрываются
8. ✅ Waypoints для edges формируют корректные линии

## 📂 Работа с файловой структурой

### Именование файлов

**КРИТИЧЕСКИ ВАЖНО: Связанные имена файлов для каждого процесса**

Для каждого процесса создается НАБОР из 4 файлов со СВЯЗАННЫМИ именами:

1. **RACI матрица** (Этап 2):
   - Формат: `[Название_процесса]_RACI.md`
   - Пример: `Жизненный_цикл_договора_RACI.md`

2. **Текстовый пайплайн** (Этап 3):
   - Формат: `[Название_процесса]_Pipeline.md`
   - Пример: `Жизненный_цикл_договора_Pipeline.md`

3. **BPMN диаграмма** (Этап 4):
   - Формат: `[Название_процесса].bpmn`
   - Пример: `Жизненный_цикл_договора.bpmn`

4. **MD документация** (Этап 4):
   - Формат: `[Название_процесса].md`
   - Пример: `Жизненный_цикл_договора.md`

**ПРАВИЛО СВЯЗИ:**
- Базовое название `[Название_процесса]` ОДИНАКОВОЕ для всех 4 файлов
- Различаются только суффиксы: `_RACI`, `_Pipeline`, `.bpmn`, `.md`
- Это обеспечивает легкую навигацию и понимание связей между файлами

**Версионирование:**
- При создании новой версии процесса добавлять номер: `_v2`, `_v3` и т.д.
- Пример: `Жизненный_цикл_договора_v2_RACI.md`
- Старые версии перемещать в `archive/`

### Архивирование
- Старые версии перемещать в папку `archive/`
- Сохранять PNG скриншоты в `archive/`
- Не удалять исходные документы из `input_data/`

### Структура папок
```
/
├── input_data/                              # Исходники (не трогать!)
│   └── Регламент_процесса.pdf
│
├── archive/                                 # Старые версии
│   ├── Процесс_v1_RACI.md
│   ├── Процесс_v1_Pipeline.md
│   ├── Процесс_v1.bpmn
│   └── Процесс_v1.md
│
├── [Название_процесса]_RACI.md             # RACI матрица (Этап 2)
├── [Название_процесса]_Pipeline.md         # Текстовый пайплайн (Этап 3)
├── [Название_процесса].bpmn                # BPMN диаграмма (Этап 4)
├── [Название_процесса].md                  # MD документация (Этап 4)
│
├── Changelog.md                             # История изменений проекта
├── README.md                                # Описание проекта
└── .cursorrules                             # Этот файл (правила)
```

**Пример для процесса "Жизненный цикл договора":**
```
/
├── Жизненный_цикл_договора_RACI.md         # RACI матрица
├── Жизненный_цикл_договора_Pipeline.md     # Пайплайн
├── Жизненный_цикл_договора.bpmn            # BPMN
└── Жизненный_цикл_договора.md              # Документация
```

## 🚫 Что НЕ делать

1. ❌ НЕ создавать BPMN файлы без секции bpmndi:BPMNDiagram
2. ❌ НЕ использовать теги `<bpmn:documentation>` для длинных описаний
3. ❌ НЕ дублировать закрывающие теги
4. ❌ НЕ создавать элементы без визуальных координат
5. ❌ НЕ удалять исходные файлы без явного запроса
6. ❌ НЕ перезаписывать рабочие файлы без создания резервной копии
7. ❌ НЕ игнорировать ошибки парсинга из Camunda Modeler

## ✅ Критерии качества результата

### BPMN файл считается правильным, если:
1. ✅ Открывается в Camunda Modeler без ошибок
2. ✅ Все элементы видны на диаграмме
3. ✅ Структура соответствует исходному документу
4. ✅ Связи между элементами логичны и полны
5. ✅ Lanes организованы по ролям/подразделениям

### MD файл считается правильным, если:
1. ✅ Названия элементов совпадают с BPMN
2. ✅ Таблицы структурированы и читаемы
3. ✅ Включены все детали: сроки, ответственные, условия
4. ✅ Есть ссылки на пункты регламента
5. ✅ Легко найти описание любого элемента процесса

## 🔄 Workflow работы

1. **Получение документа** → Анализ формата
2. **Извлечение текста** → OCR если нужно
3. **Парсинг структуры** → Определение элементов процесса
4. **Генерация BPMN XML** → С ПОЛНОЙ визуальной информацией
5. **Генерация MD** → Таблицы с деталями
6. **Валидация** → Проверка в Camunda Modeler
7. **Архивирование** → Старых версий при изменениях

## 📝 Примеры команд для выполнения

### При создании нового BPMN
```
1. Создать структуру XML с namespace
2. Добавить collaboration и participant
3. Создать process с laneSet
4. Для КАЖДОГО элемента создать:
   - Определение в process
   - BPMNShape в bpmndi:BPMNDiagram
5. Для КАЖДОГО flow создать:
   - sequenceFlow в process
   - BPMNEdge в bpmndi:BPMNDiagram
6. Сохранить файл
7. Создать соответствующий MD файл
```

## 🎓 Обучение на примерах

**Успешный пример:** `Жизненный_цикл_договора_1.bpmn`
- 18 элементов процесса
- 6 дорожек (lanes)
- Все элементы имеют визуальное представление
- Открывается в Camunda без ошибок

**Документация:** `Жизненный_цикл_договора_1.md`
- 8 структурированных таблиц
- Детальные описания с условиями
- Ссылки на пункты регламента РД-Б7.004-05

---

## 📁 ОРГАНИЗАЦИЯ ФАЙЛОВОЙ СТРУКТУРЫ ПРОЕКТА

### Структура папок

```
/home/budnik_an/Obligations/
│
├── input/                           # Входные документы (READ-ONLY для AI)
│   ├── ДП-Б6.001-07.pdf
│   ├── ДП-М1.020-06.pdf
│   ├── КД-СТ-161-01.pdf
│   └── Руководство_по_качеству.pdf
│
├── output/                          # Результаты обработки (WORKSPACE для AI)
│   ├── ДП-Б6.001-07/               # Папка создается автоматически при обработке
│   │   ├── ДП-Б6.001-07_OCR.md            # Расшифровка текста (OCR)
│   │   ├── ДП-Б6.001-07_OCR.pdf           # PDF копия OCR
│   │   ├── ДП-Б6.001-07_RACI.md           # RACI матрица процесса
│   │   ├── ДП-Б6.001-07_RACI.pdf          # PDF копия RACI
│   │   ├── ДП-Б6.001-07_Pipeline.md       # Текстовый пайплайн
│   │   ├── ДП-Б6.001-07_Pipeline.pdf      # PDF копия Pipeline
│   │   ├── ДП-Б6.001-07.bpmn              # BPMN диаграмма
│   │   ├── ДП-Б6.001-07.md                # Документация процесса
│   │   └── ДП-Б6.001-07.pdf               # PDF копия документации
│   │
│   └── ДП-М1.020-06/               # Другой документ
│       └── ...
│
├── scripts/                         # Рабочий код проекта
│   └── pdf_to_context/             # Основной модуль обработки PDF
│       ├── pipeline.py             # Главный пайплайн
│       ├── core/                   # Ядро системы
│       ├── extractors/             # Извлечение данных
│       ├── ir/                     # Промежуточное представление
│       ├── models/                 # Модели данных
│       └── ocr_service/            # OCR интеграция
│
├── archive/                         # Архив старых версий
│   ├── old_output/                 # Старые результаты
│   ├── old_docs/                   # Старая документация
│   └── tests/                      # Тестовые скрипты
│
├── DeepSeek-OCR/                   # Модуль DeepSeek OCR
│
├── docs/                           # Документация проекта
│   ├── Architecture.md
│   └── DeepSeek_OCR_Setup.md
│
├── .cursorrules                    # Правила работы AI
├── README.md                       # Описание проекта
├── Changelog.md                    # История изменений
└── requirements.txt                # Зависимости Python
```

### Правила работы с папками

#### 🔒 input/ (READ-ONLY для AI)
- **Назначение**: Хранение исходных PDF документов для обработки
- **Доступ AI**: Только чтение
- **ЗАПРЕЩЕНО**: Создание, изменение, удаление файлов
- **Управление**: Только пользователь вручную добавляет документы

#### ⚙️ output/ (WORKSPACE для AI)
- **Назначение**: Результаты автоматической обработки документов
- **Структура**: Папка создается автоматически при обработке документа
- **Название папки**: Имя документа без расширения и без спецсимволов
- **Стандартный набор файлов** (для каждого документа):
  1. `[Название]_OCR.md` + `[Название]_OCR.pdf` - Расшифровка текста
  2. `[Название]_RACI.md` + `[Название]_RACI.pdf` - RACI матрица
  3. `[Название]_Pipeline.md` + `[Название]_Pipeline.pdf` - Текстовый пайплайн
  4. `[Название].bpmn` - BPMN диаграмма (только XML)
  5. `[Название].md` + `[Название].pdf` - Документация процесса

#### 💾 scripts/ (Рабочий код)
- **Назначение**: Весь функциональный код проекта
- **Содержимое**: Модули обработки, пайплайны, утилиты
- **Доступ AI**: Чтение и запись при разработке

#### 📦 archive/ (Архив)
- **Назначение**: Хранение старых версий, тестов, эксперментов
- **Содержимое**: Всё, что не является актуальным рабочим кодом или результатами
- **Доступ AI**: Только чтение, при необходимости

---

## 🔄 WORKFLOW ОБРАБОТКИ ДОКУМЕНТА

### Команда пользователя
```
"обработай документ [название]"
```

### Последовательность действий AI

#### ШАГ 1: Проверка и подготовка
```python
1. Проверить наличие файла в input/[название].pdf
2. Извлечь базовое имя документа (без расширения)
3. Очистить имя: убрать скобки, спецсимволы, заменить пробелы на _
   Пример: "ДП-М1.020-06 (Эталон №14).pdf" → "ДП-М1.020-06"
4. Создать папку output/[базовое_имя]/ если не существует
```

#### ШАГ 2: OCR обработка
```python
1. Запустить скрипт DeepSeek OCR
   Команда: python scripts/pdf_to_context/pipeline.py input/[название].pdf
2. Сохранить результат в output/[базовое_имя]/[базовое_имя]_OCR.md
3. Создать PDF копию: [базовое_имя]_OCR.pdf (используя pandoc или weasyprint)
4. Проверить успешность: файл создан и не пустой
```

#### ШАГ 3: Создание RACI матрицы
```python
1. Прочитать [базовое_имя]_OCR.md
2. Следовать методологии из раздела "Этап 2: МАТРИЦА ОТВЕТСТВЕННОСТИ RACI"
3. Создать файл [базовое_имя]_RACI.md
4. Создать PDF копию: [базовое_имя]_RACI.pdf
5. Согласовать с пользователем структуру ролей
```

#### ШАГ 4: Создание текстового пайплайна
```python
1. На основе OCR и RACI
2. Следовать методологии из раздела "Этап 3: ТЕКСТОВЫЙ ПАЙПЛАЙН ПРОЦЕССА"
3. Создать файл [базовое_имя]_Pipeline.md
4. Создать PDF копию: [базовое_имя]_Pipeline.pdf
```

#### ШАГ 5: Создание BPMN и документации
```python
1. На основе Pipeline и RACI
2. Следовать методологии из раздела "Этап 4: СОЗДАНИЕ BPMN И ДОКУМЕНТАЦИИ"
3. Создать файл [базовое_имя].bpmn (только XML, без PDF)
4. Создать файл [базовое_имя].md (документация)
5. Создать PDF копию документации: [базовое_имя].pdf
6. Валидировать BPMN в Camunda Modeler
```

#### ШАГ 6: Генерация PDF копий
```python
# Для каждого MD файла создать PDF версию
# Используя один из инструментов:
- pandoc: pandoc input.md -o output.pdf --pdf-engine=xelatex
- weasyprint: weasyprint input.md output.pdf
- markdown-pdf: npm package для конвертации

# ВАЖНО: 
- Сохранять форматирование таблиц
- Использовать русские шрифты (DejaVu Sans, Liberation Sans)
- Сохранять структуру заголовков
```

#### ШАГ 7: Отчет
```markdown
Вывести в чат:

✅ ОБРАБОТКА ЗАВЕРШЕНА: [Название документа]

📁 Созданные файлы в output/[базовое_имя]/:
1. ✓ [базовое_имя]_OCR.md + PDF
2. ✓ [базовое_имя]_RACI.md + PDF
3. ✓ [базовое_имя]_Pipeline.md + PDF
4. ✓ [базовое_имя].bpmn
5. ✓ [базовое_имя].md + PDF

📊 Статистика процесса:
- Количество страниц: X
- Количество ролей: Y
- Количество задач: Z
- Количество шлюзов: W
- Общее время процесса: T дней

⚠️ Выявленные проблемы:
- [Список проблем или неясностей]

💡 Рекомендации:
- [Предложения по доработке]

🔍 Следующие шаги:
- Проверить BPMN в Camunda Modeler
- Уточнить неясные моменты
- При необходимости доработать процесс
```

### Правила именования файлов

#### Базовое имя документа
```python
# Алгоритм очистки имени:
1. Взять имя файла без расширения
2. Убрать содержимое в скобках: "(Эталон №14)" → ""
3. Убрать спецсимволы кроме дефиса и подчеркивания: "№14" → "14"
4. Заменить пробелы на подчеркивания: " " → "_"
5. Удалить повторяющиеся подчеркивания: "__" → "_"
6. Удалить подчеркивания в начале/конце

# Примеры:
"ДП-М1.020-06 (Эталон №14 для ознакомления).pdf" → "ДП-М1.020-06"
"КД-СТ-161-01 (Эталон для ознакомления).pdf" → "КД-СТ-161-01"
"Руководство по качеству.pdf" → "Руководство_по_качеству"
```

#### Связанные имена файлов
```
Базовое имя: ДП-М1.020-06

Файлы в output/ДП-М1.020-06/:
├── ДП-М1.020-06_OCR.md           # Расшифровка
├── ДП-М1.020-06_OCR.pdf
├── ДП-М1.020-06_RACI.md          # RACI матрица
├── ДП-М1.020-06_RACI.pdf
├── ДП-М1.020-06_Pipeline.md      # Пайплайн
├── ДП-М1.020-06_Pipeline.pdf
├── ДП-М1.020-06.bpmn             # BPMN (только XML)
├── ДП-М1.020-06.md               # Документация
└── ДП-М1.020-06.pdf
```

### Генерация PDF из Markdown

**ОБЯЗАТЕЛЬНО создавать PDF копии всех MD файлов**

#### Рекомендуемый инструмент: pandoc

```bash
# Установка (если нужна):
sudo apt-get install pandoc texlive-xetex texlive-fonts-recommended

# Конвертация с поддержкой русского языка:
pandoc input.md -o output.pdf \
  --pdf-engine=xelatex \
  -V mainfont="DejaVu Sans" \
  -V geometry:margin=2cm \
  --toc

# Для таблиц RACI:
pandoc RACI.md -o RACI.pdf \
  --pdf-engine=xelatex \
  -V mainfont="DejaVu Sans" \
  -V geometry:margin=1.5cm,landscape \
  --toc
```

#### Альтернатива: weasyprint

```bash
# Установка:
pip install weasyprint

# Конвертация:
weasyprint input.md output.pdf
```

#### Правила генерации PDF:
1. Сохранять форматирование таблиц
2. Использовать шрифты с поддержкой кириллицы
3. Для широких таблиц (RACI) использовать landscape ориентацию
4. Включать оглавление (TOC) для длинных документов
5. Поля: 1.5-2 см для читаемости
6. Проверять читаемость таблиц в PDF

---

## 🎯 Итоговый чеклист для каждого процесса

### Этап 0: Архитектурный анализ (для программных компонентов)
- [ ] Проведен анализ архитектуры решения
- [ ] Определены компоненты и модули
- [ ] Выявлены зависимости и интерфейсы
- [ ] Оценена сложность и риски
- [ ] Предложены варианты реализации
- [ ] Обоснован выбор подхода
- [ ] **ПОЛУЧЕНО СОГЛАСИЕ ПОЛЬЗОВАТЕЛЯ** на архитектуру
- [ ] Соблюдены принципы SOLID/KISS/DRY

### Этап 1: Анализ и структурирование (для процессов)
- [ ] Проведен анализ исходных документов
- [ ] Определен тип процесса
- [ ] Определены границы процесса
- [ ] Предложена структура работы по разделам
- [ ] **ПОЛУЧЕНО СОГЛАСИЕ ПОЛЬЗОВАТЕЛЯ** на структуру

### Этап 2: RACI матрица
- [ ] Создан файл `[Название_процесса]_RACI.md`
- [ ] Матрица содержит все activities процесса
- [ ] Определены все роли (R, A, C, I)
- [ ] Структура Pool и Lanes согласована с пользователем

### Этап 3: Текстовый пайплайн
- [ ] Создан файл `[Название_процесса]_Pipeline.md`
- [ ] Каждый activity описан по 7 пунктам
- [ ] Указаны способы передачи данных
- [ ] Указано время исполнения
- [ ] Определены метрики оценки исполнения
- [ ] Создан сводный пайплайн процесса

### Этап 4: BPMN и документация
- [ ] Создан файл `[Название_процесса].bpmn`
- [ ] Создан файл `[Название_процесса].md` (то же имя!)
- [ ] Создана PDF копия `[Название_процесса].pdf`
- [ ] Все namespace объявлены в BPMN
- [ ] Структура: definitions → collaboration → process
- [ ] LaneSet с корректными flowNodeRef
- [ ] Каждый элемент имеет BPMNShape с координатами
- [ ] Каждый flow имеет BPMNEdge с waypoints
- [ ] Нет дублирующих тегов
- [ ] Файл открывается в Camunda Modeler
- [ ] Все элементы видны на диаграмме
- [ ] Названия в MD точно соответствуют BPMN
- [ ] Старые версии перенесены в archive/

### Финальная проверка PDF
- [ ] Создана PDF копия OCR: `[Название]_OCR.pdf`
- [ ] Создана PDF копия RACI: `[Название]_RACI.pdf`
- [ ] Создана PDF копия Pipeline: `[Название]_Pipeline.pdf`
- [ ] Создана PDF копия документации: `[Название].pdf`
- [ ] Все PDF файлы читаемы и корректно отформатированы
- [ ] Таблицы в PDF не обрезаны
- [ ] Русский текст отображается корректно

### Связь файлов
- [ ] Все 4 файла имеют связанное базовое имя
- [ ] `[Название]_RACI.md` + `[Название]_Pipeline.md` + `[Название].bpmn` + `[Название].md`
- [ ] Легко найти все материалы по процессу

**При любых сомнениях - проверить в Camunda Modeler!**

