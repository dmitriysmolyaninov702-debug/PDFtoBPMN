# 🛠️ Инструменты Cursor AI: Полное руководство

Это руководство описывает все инструменты (tools), доступные AI-ассистенту в Cursor IDE.

---

## 📚 Категории инструментов

### 1️⃣ ПОИСК И НАВИГАЦИЯ

#### `codebase_search` - Семантический поиск по коду
**Назначение:** Находит код по смыслу, а не по точному тексту.

**Когда использовать:**
- ✅ Исследование незнакомого кодбейса
- ✅ Вопросы "как/где/что" для понимания поведения
- ✅ Поиск по смыслу, а не по точному тексту

**Когда НЕ использовать:**
- ❌ Точное совпадение текста → используй `grep`
- ❌ Чтение известных файлов → используй `read_file`
- ❌ Простой поиск символов → используй `grep`
- ❌ Поиск файла по имени → используй `glob_file_search`

**Примеры:**
```
✅ ХОРОШО: "Where is interface MyInterface implemented in the frontend?"
✅ ХОРОШО: "Where do we encrypt user passwords before saving?"
❌ ПЛОХО: "MyInterface frontend" (слишком расплывчато)
❌ ПЛОХО: "AuthService" (одно слово → используй grep)
```

**Стратегия использования:**
1. Начать с исследовательских запросов (широко, `[]` если не уверен где)
2. Просмотреть результаты; если выделяется директория → повторить с таргетом
3. Разбить большие вопросы на меньшие
4. Для больших файлов (>1K строк) использовать `codebase_search` вместо чтения целиком

---

#### `grep` - Мощный поиск на основе ripgrep
**Назначение:** Точный поиск текста/regex по файлам.

**Параметры:**
- `pattern` - регулярное выражение для поиска
- `path` - файл или директория (по умолчанию workspace)
- `output_mode` - "content" (строки), "files_with_matches" (пути), "count" (счетчики)
- `-i` - case insensitive
- `-A/-B/-C` - контекст после/до/вокруг (только для content mode)
- `glob` - фильтр по типу файла (например "*.js")
- `type` - тип файла (js, py, rust, go и т.д.)
- `multiline` - паттерн может охватывать несколько строк

**Примеры:**
```bash
# Подсчет элементов
grep -c "<bpmn:task" file.bpmn

# Поиск с контекстом
grep "import OCRPrompts" -C 3 app.py

# Case insensitive
grep -i "error" logs/

# Только имена файлов
grep --output_mode files_with_matches "TODO"

# Multiline паттерн
grep --multiline "struct \\{[\\s\\S]*?field" 
```

**Когда использовать:**
- ✅ Точное совпадение символов/строк
- ✅ Подсчет элементов в файлах
- ✅ Поиск дубликатов
- ✅ Regex паттерны

---

#### `glob_file_search` - Поиск файлов по паттерну
**Назначение:** Быстрый поиск файлов по имени/паттерну.

**Примеры:**
```bash
# Найти все BPMN файлы
glob_file_search "*.bpmn"

# Найти тестовые файлы
glob_file_search "**/test_*.py"

# Найти конфигурацию OCR
glob_file_search "**/ocr_service/**/*.py"
```

---

#### `list_dir` - Список файлов в директории
**Назначение:** Просмотр содержимого папки.

**Параметры:**
- `target_directory` - путь к директории
- `ignore_globs` - паттерны для игнорирования (например "node_modules")

**Примеры:**
```python
list_dir("/home/user/project")
list_dir("/home/user/project", ignore_globs=["*.pyc", "**/__pycache__/**"])
```

---

### 2️⃣ ЧТЕНИЕ И АНАЛИЗ

#### `read_file` - Чтение файлов
**Назначение:** Получить содержимое файла.

**Параметры:**
- `target_file` - путь к файлу
- `offset` - начальная строка (для больших файлов)
- `limit` - количество строк для чтения

**Примеры:**
```python
# Прочитать весь файл
read_file("README.md")

# Прочитать фрагмент (строки 100-200)
read_file("large_file.py", offset=100, limit=100)
```

**Форматы:**
- ✅ Текстовые файлы (с нумерацией строк)
- ✅ Изображения (jpeg, png, gif, webp)

**Важно:** 
- Строки нумеруются с 1
- Формат: `LINE_NUMBER|LINE_CONTENT`
- Можно читать несколько файлов параллельно

---

#### `read_lints` - Чтение ошибок линтера
**Назначение:** Получить список ошибок линтера из workspace.

**Параметры:**
- `paths` - массив путей к файлам/директориям (опционально)

**Примеры:**
```python
# Все ошибки в workspace
read_lints()

# Ошибки в конкретном файле
read_lints(["src/components/Button.tsx"])

# Ошибки в директории
read_lints(["backend/api/"])
```

**⚠️ Важно:** Не вызывать на файлах, которые не редактировал!

---

### 3️⃣ РЕДАКТИРОВАНИЕ ФАЙЛОВ

#### `search_replace` - Точная замена строк
**Назначение:** Замена текста в существующих файлах.

**Параметры:**
- `file_path` - путь к файлу
- `old_string` - текст для замены (ДОЛЖЕН быть уникальным!)
- `new_string` - новый текст
- `replace_all` - заменить все вхождения (по умолчанию false)

**КРИТИЧЕСКИЕ ПРАВИЛА:**
```python
# ❌ ПЛОХО - короткий контекст (может быть несколько совпадений)
search_replace("file.py", "import os", "import sys")

# ✅ ХОРОШО - достаточный контекст (5-10 строк до/после)
search_replace("file.py", 
    old_string="""
import os
import sys
from pathlib import Path

def process_file(path):
    return os.path.exists(path)
""",
    new_string="""
import os
import sys
from pathlib import Path

def process_file(path):
    return Path(path).exists()
""")
```

**Для BPMN XML файлов:**
- ✅ ВСЕГДА включать 5-10 строк контекста ДО и ПОСЛЕ
- ✅ Использовать уникальные фрагменты
- ✅ Проверять точное соответствие whitespace

---

#### `write` - Создание/перезапись файлов
**Назначение:** Создать новый файл или полностью перезаписать существующий.

**Параметры:**
- `file_path` - путь к файлу
- `contents` - содержимое файла

**⚠️ ВАЖНО:**
- Если файл существует → ОБЯЗАТЕЛЬНО сначала прочитать его через `read_file`
- ВСЕГДА предпочитать редактирование существующих файлов (не создавать новые без необходимости)
- НЕ создавать документацию (*.md) или README без явного запроса

**Примеры:**
```python
# Создать новый файл
write("new_script.py", "#!/usr/bin/env python3\n\nprint('Hello')")

# ❌ ПЛОХО - перезапись без чтения
write("existing_file.py", "new content")

# ✅ ХОРОШО - сначала читаем
read_file("existing_file.py")
# ... анализируем ...
write("existing_file.py", "updated content")
```

---

#### `delete_file` - Удаление файлов
**Назначение:** Удалить файл.

**Параметры:**
- `target_file` - путь к файлу
- `explanation` - зачем удаляем (опционально)

**Примеры:**
```python
delete_file("temp_file.txt", explanation="Временный файл больше не нужен")
```

---

#### `edit_notebook` - Редактирование Jupyter notebooks
**Назначение:** Редактировать ячейки в .ipynb файлах.

**Параметры:**
- `target_notebook` - путь к notebook
- `cell_idx` - индекс ячейки (0-based)
- `is_new_cell` - создать новую ячейку (true) или редактировать существующую (false)
- `cell_language` - язык ячейки (python, markdown, javascript, typescript, r, sql, shell, raw, other)
- `old_string` - текст для замены (если редактируем)
- `new_string` - новый текст

**КРИТИЧЕСКИЕ ПРАВИЛА:**
- ✅ Использовать ТОЛЬКО для notebooks
- ✅ Включать 3-5 строк контекста ДО и ПОСЛЕ
- ✅ old_string и new_string БЕЗ JSON синтаксиса
- ✅ ВСЕГДА предоставлять ВСЕ обязательные аргументы

---

### 4️⃣ ВЫПОЛНЕНИЕ КОМАНД

#### `run_terminal_cmd` - Запуск команд в терминале
**Назначение:** Выполнить shell команду.

**Параметры:**
- `command` - команда для выполнения
- `explanation` - зачем выполняем (опционально)
- `is_background` - запустить в фоне (для долгих процессов)

**ВАЖНЫЕ ПРАВИЛА:**
```bash
# 1. Использовать неинтерактивные флаги
npx --yes some-package  # НЕ запрашивать подтверждение

# 2. Фоновые процессы
nohup python server.py > log.txt 2>&1 &  # is_background=true

# 3. НЕ использовать для чтения/редактирования файлов
# ❌ ПЛОХО: cat file.txt
# ✅ ХОРОШО: read_file("file.txt")

# 4. НЕ использовать cat/sed/awk для файловых операций
# ❌ ПЛОХО: echo "text" > file.txt
# ✅ ХОРОШО: write("file.txt", "text")

# 5. НИКОГДА не обновлять git config
# 6. НИКОГДА не делать git push --force (без явного запроса)
# 7. НИКОГДА не коммитить без явного запроса
```

**Контекст shell:**
- Если "new shell" → `cd` в нужную директорию
- Если "same shell" → окружение сохраняется (venv, exported vars)

---

### 5️⃣ УПРАВЛЕНИЕ ЗАДАЧАМИ

#### `todo_write` - Управление TODO списком
**Назначение:** Создать и отслеживать задачи.

**Параметры:**
- `merge` - объединить с существующими TODO (true) или заменить (false)
- `todos` - массив задач

**Формат задачи:**
```json
{
  "id": "unique_id",
  "content": "Описание задачи",
  "status": "pending" | "in_progress" | "completed" | "cancelled"
}
```

**Когда использовать:**
- ✅ Сложные многошаговые задачи (3+ шага)
- ✅ Нетривиальные задачи требующие планирования
- ✅ Пользователь явно запросил TODO список
- ✅ Несколько задач от пользователя

**Когда НЕ использовать:**
- ❌ Одиночные простые задачи
- ❌ Тривиальные задачи (< 3 шагов)
- ❌ Чисто информационные запросы
- ❌ НЕ включать: linting, testing, поиск по коду

**Правила:**
- Только ОДНА задача в статусе `in_progress`
- Обновлять статус в реальном времени
- Отмечать завершенные задачи НЕМЕДЛЕННО
- Создавать конкретные, выполнимые задачи

**Пример:**
```python
todo_write(merge=False, todos=[
    {"id": "1", "content": "Добавить state management", "status": "in_progress"},
    {"id": "2", "content": "Реализовать темные стили", "status": "pending"},
    {"id": "3", "content": "Создать toggle компонент", "status": "pending"},
    {"id": "4", "content": "Обновить компоненты", "status": "pending"}
])
```

---

### 6️⃣ ДОПОЛНИТЕЛЬНЫЕ ИНСТРУМЕНТЫ

#### `web_search` - Поиск в интернете
**Назначение:** Получить актуальную информацию из интернета.

**Параметры:**
- `search_term` - запрос для поиска
- `explanation` - зачем ищем

**Когда использовать:**
- ✅ Актуальная информация (не в тренировочных данных)
- ✅ Верификация текущих фактов
- ✅ Технические обновления (версии, API)

**Примеры:**
```python
web_search("PaddleOCR latest version", "Проверка последней версии библиотеки")
web_search("Cursor IDE release notes 2025", "Узнать последние изменения")
```

---

#### `update_memory` - Управление памятью
**Назначение:** Сохранить/обновить/удалить долгосрочную память.

**Параметры:**
- `action` - "create", "update", "delete"
- `title` - заголовок памяти
- `knowledge_to_store` - текст памяти (до 1 абзаца)
- `existing_knowledge_id` - ID для update/delete

**Правила:**
- ✅ Создавать ТОЛЬКО если пользователь явно попросил "запомни"
- ✅ Обновлять при дополнении существующей памяти
- ✅ Удалять при противоречии существующей памяти

**Пример:**
```python
# Создать память
update_memory(
    action="create",
    title="Предпочтения OCR",
    knowledge_to_store="Пользователь предпочитает PaddleOCR для CPU систем"
)

# Обновить память
update_memory(
    action="update",
    existing_knowledge_id="abc123",
    title="Предпочтения OCR",
    knowledge_to_store="Пользователь использует DeepSeek-OCR на GPU системах"
)
```

---

## 🎯 ЛУЧШИЕ ПРАКТИКИ

### 1. Параллельное выполнение
**Если инструменты независимы → вызывать ПАРАЛЛЕЛЬНО:**

```python
# ✅ ХОРОШО - параллельно читаем 3 файла
read_file("file1.py")
read_file("file2.py")
read_file("file3.py")

# ❌ ПЛОХО - последовательно (медленно)
# read_file("file1.py")
# ... ждем ...
# read_file("file2.py")
```

### 2. Выбор правильного инструмента

```
Задача                          → Инструмент
─────────────────────────────────────────────────────
Найти код по смыслу             → codebase_search
Точное совпадение текста        → grep
Подсчет элементов               → grep -c
Найти файл по имени             → glob_file_search
Прочитать файл                  → read_file
Заменить текст                  → search_replace
Создать файл                    → write
Выполнить команду               → run_terminal_cmd
Проверить ошибки                → read_lints
```

### 3. Работа с большими файлами

```python
# ❌ ПЛОХО - читаем весь файл 5000 строк
read_file("huge.xml")

# ✅ ХОРОШО - читаем частями
read_file("huge.xml", offset=1, limit=100)
read_file("huge.xml", offset=2500, limit=100)

# ИЛИ использовать grep для поиска конкретных секций
grep "specific_section" huge.xml -A 50 -B 10
```

### 4. BPMN и XML файлы

```python
# ИТЕРАТИВНЫЙ ПОДХОД для BPMN:
# Этап 1: write - базовая структура
write("process.bpmn", basic_structure)

# Этап 2-5: search_replace - добавление элементов
search_replace("process.bpmn", old_string=context, new_string=updated)

# Этап 6: валидация
grep -c "<bpmn:task" process.bpmn
grep -c "BPMNShape" process.bpmn
```

---

## 📊 СТАТИСТИКА И ОГРАНИЧЕНИЯ

### Ограничения инструментов
- `grep`: Результаты обрезаются для отзывчивости (показывает "at least" если много)
- `read_file`: Для огромных файлов использовать offset/limit
- `search_replace`: old_string ДОЛЖЕН быть уникальным в файле
- `run_terminal_cmd`: Пользователь может отклонить/изменить команду

### Бюджет токенов
- Доступно: 1,000,000 токенов
- При достижении лимита → автоматический fresh context (с сохранением прогресса)
- Можно делать 200+ tool calls для сложных задач

---

## 🚀 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Пример 1: Рефакторинг кода
```python
# 1. Найти все использования старой функции
codebase_search("Where is old_function used?", target_directories=[])

# 2. Подсчитать количество
grep -c "old_function" --output_mode count

# 3. Для каждого файла - заменить
search_replace("file1.py", 
    old_string="old context\nold_function()\nmore context",
    new_string="old context\nnew_function()\nmore context")

# 4. Проверить ошибки
read_lints(["file1.py"])
```

### Пример 2: Создание новой фичи
```python
# 1. Создать TODO
todo_write(merge=False, todos=[
    {"id": "1", "content": "Создать модель", "status": "in_progress"},
    {"id": "2", "content": "Создать API endpoint", "status": "pending"},
    {"id": "3", "content": "Добавить тесты", "status": "pending"}
])

# 2. Создать файл модели
write("models/user.py", model_code)

# 3. Обновить TODO
todo_write(merge=True, todos=[
    {"id": "1", "status": "completed"},
    {"id": "2", "status": "in_progress"}
])

# 4. Создать API
write("api/users.py", api_code)

# 5. Финализировать TODO
todo_write(merge=True, todos=[
    {"id": "2", "status": "completed"},
    {"id": "3", "status": "in_progress"}
])
```

### Пример 3: Анализ и документация
```python
# 1. Найти все OCR сервисы
codebase_search("What OCR services are implemented?", target_directories=["scripts/pdf_to_context/ocr_service/"])

# 2. Прочитать конкретные файлы
read_file("scripts/pdf_to_context/ocr_service/base.py")
read_file("scripts/pdf_to_context/ocr_service/paddleocr_service.py")

# 3. Создать документацию
write("docs/OCR_Services_Architecture.md", documentation)

# 4. Добавить в README
search_replace("README.md",
    old_string="## OCR Services\n\n(placeholder)",
    new_string="## OCR Services\n\nSee [OCR_Services_Architecture.md](docs/OCR_Services_Architecture.md)")
```

---

## ⚠️ ЧАСТЫЕ ОШИБКИ

### ❌ Ошибка 1: Короткий контекст в search_replace
```python
# ПЛОХО - может быть несколько совпадений
search_replace("file.py", "import os", "import sys")

# ХОРОШО - уникальный контекст
search_replace("file.py", 
    "import os\nimport sys\nfrom pathlib import Path\n\ndef main():",
    "import sys\nfrom pathlib import Path\n\ndef main():")
```

### ❌ Ошибка 2: Использование terminal вместо специализированных инструментов
```python
# ПЛОХО
run_terminal_cmd("cat file.txt")

# ХОРОШО
read_file("file.txt")
```

### ❌ Ошибка 3: Создание TODO для тривиальных задач
```python
# ПЛОХО - слишком просто
todo_write(merge=False, todos=[
    {"id": "1", "content": "Добавить комментарий", "status": "in_progress"}
])

# ХОРОШО - для сложных задач
todo_write(merge=False, todos=[
    {"id": "1", "content": "Рефакторинг OCR архитектуры", "status": "in_progress"},
    {"id": "2", "content": "Создать Factory pattern", "status": "pending"},
    {"id": "3", "content": "Добавить CPU fallback", "status": "pending"}
])
```

---

## 📖 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- Официальная документация Cursor: https://docs.cursor.com
- Примеры использования: см. историю чата
- Best practices: см. `.cursorrules` в проекте

---

**Версия:** 1.0  
**Дата:** 09-11-2025  
**Автор:** Cursor AI Assistant

